export default {
    math: {
        add(a: number, b: number) {
            return a + b;
        },
        subtract(a: number, b: number) {
            return a - b;
        },
    },
    greet(name: string) {
        return `Hello, ${name}!`;
    },
};
